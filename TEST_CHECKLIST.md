# BlockTodo 로컬 테스트 체크리스트

> **실행 전 준비**
> ```bash
> cd "D:\blockchain\to-do_list\to-do_list\frontend"
> npm run build
> npm run pages:dev
> ```
> 접속: http://localhost:4173

---

## A. 지갑 연결

| # | 테스트 항목 | 기대 결과 | 결과 |
|---|-------------|-----------|------|
| A-1 | MetaMask 미연결 상태로 앱 접속 | 랜딩 화면(SCR-01) 표시, "MetaMask로 연결하기" 버튼 보임 | |
| A-2 | "MetaMask로 연결하기" 버튼 클릭 | MetaMask 팝업 열림 | |
| A-3 | MetaMask에서 **거부** | "지갑 연결을 취소했습니다." 표시, 앱 정상 유지 | |
| A-4 | MetaMask에서 **승인** (Sepolia 네트워크) | 메인 화면(SCR-03) 진입, 헤더에 주소 표시 | |
| A-5 | MetaMask를 다른 네트워크(예: Mainnet)로 전환 | 경고 화면(SCR-02) 표시, "Sepolia 전환하기" 버튼 보임 | |
| A-6 | SCR-02에서 "Sepolia 전환하기" 클릭 | SCR-03으로 복귀 | |
| A-7 | 헤더 "연결 해제" 클릭 | SCR-01(랜딩)으로 복귀 | |

---

## B. 카테고리 관리

| # | 테스트 항목 | 기대 결과 | 결과 |
|---|-------------|-----------|------|
| B-1 | 사이드바 "카테고리 관리" 클릭 | 카테고리 관리 모달(SCR-07) 열림 | |
| B-2 | 빈 이름으로 카테고리 추가 시도 | 추가되지 않음 | |
| B-3 | "업무" 카테고리 추가 | MetaMask 팝업 → 승인 → TxPending → 완료 후 목록에 "업무" 표시 | |
| B-4 | "개인" 카테고리 추가 | 동일 흐름, 목록에 "개인" 추가 | |
| B-5 | 카테고리 추가 중 MetaMask 거부 | 모달 그대로 유지, TxPending 사라짐 | |
| B-6 | 사이드바에서 카테고리 클릭 | 해당 카테고리 필터 적용됨 | |
| B-7 | Todo가 있는 카테고리 삭제 시도 | 에러: "이 카테고리에 할 일이 있어 삭제할 수 없습니다." | |
| B-8 | Todo가 없는 카테고리 삭제 | 삭제 트랜잭션 → 목록에서 제거 | |

---

## C. 할 일 추가

| # | 테스트 항목 | 기대 결과 | 결과 |
|---|-------------|-----------|------|
| C-1 | 카테고리 없을 때 "+ 새 할 일 추가" 클릭 | "카테고리 먼저 추가" 안내 모달 | |
| C-2 | 카테고리 있을 때 추가 버튼 클릭 | TodoForm 모달(SCR-04) 열림 | |
| C-3 | 제목 없이 등록 시도 | "제목을 입력해주세요." 에러 메시지 | |
| C-4 | 마감일을 오늘 이전 날짜로 설정 후 등록 | "마감일은 오늘 이후 날짜여야 합니다." 에러 | |
| C-5 | 제목 입력 시 가스비 예상치 표시 | 폼 하단에 "예상 가스: ~XX,XXX gas" 표시 | |
| C-6 | 정상 할 일 추가 (제목 + 카테고리 + HIGH + 마감일) | MetaMask 팝업 → TxPending → 완료 → 목록에 추가됨 | |
| C-7 | 마감일 없는 할 일 추가 | 정상 추가, D-day 배지 미표시 | |
| C-8 | 마감일이 오늘인 할 일 추가 | 목록에 "D-day" 주황 배지 표시 | |
| C-9 | 마감일이 미래인 할 일 추가 | 목록에 "D-N" 파란 배지 표시 | |
| C-10 | 이미 지난 마감일로 저장된 할 일 확인 | "D+N" 빨간 배지 표시 | |

---

## D. 할 일 편집 / 삭제 / 완료

| # | 테스트 항목 | 기대 결과 | 결과 |
|---|-------------|-----------|------|
| D-1 | 할 일 카드 "편집" 클릭 | 기존 데이터가 채워진 편집 모달(SCR-05) 열림 | |
| D-2 | 제목 수정 후 "저장하기" | 트랜잭션 → 목록에 변경 반영 | |
| D-3 | 카테고리 변경 후 저장 | 변경된 카테고리로 반영, 카테고리 필터에도 적용 | |
| D-4 | 할 일 카드 "삭제" 클릭 | 삭제 확인 모달(SCR-06) 열림 | |
| D-5 | 삭제 확인 모달에서 "취소" | 모달 닫힘, 할 일 그대로 유지 | |
| D-6 | 삭제 확인 후 "삭제하기" | 트랜잭션 → 목록에서 제거 | |
| D-7 | 체크박스 클릭 (미완료 → 완료) | 체크박스 채워짐 + 제목 취소선 + 투명도 낮아짐 | |
| D-8 | 체크박스 다시 클릭 (완료 → 미완료) | 원래 스타일로 복원 | |

---

## E. 필터 & 정렬

| # | 테스트 항목 | 기대 결과 | 결과 |
|---|-------------|-----------|------|
| E-1 | 상태 필터 → "미완료" | 완료된 항목 사라짐 | |
| E-2 | 상태 필터 → "완료" | 미완료 항목 사라짐 | |
| E-3 | 카테고리 필터 드롭다운으로 특정 카테고리 선택 | 해당 카테고리 항목만 표시 | |
| E-4 | 사이드바 카테고리 클릭 후 필터 드롭다운 확인 | 드롭다운도 같은 카테고리로 연동됨 | |
| E-5 | 우선순위 필터 → "HIGH" | HIGH 항목만 표시 | |
| E-6 | 정렬 → "마감일 순" | 마감일 빠른 순 정렬, 마감일 없는 항목 맨 뒤 | |
| E-7 | 정렬 → "우선순위 순" | HIGH → MEDIUM → LOW 순 정렬 | |
| E-8 | 필터 적용 후 결과 없음 | "🔍 필터 조건에 맞는 할 일이 없습니다." 메시지 | |
| E-9 | 할 일이 하나도 없을 때 | "📝 아직 할 일이 없습니다." 메시지 | |

---

## F. 에러 처리

| # | 테스트 항목 | 기대 결과 | 결과 |
|---|-------------|-----------|------|
| F-1 | 트랜잭션 중 MetaMask 거부 | TxPending 사라짐, 앱 정상 유지, 목록 변경 없음 | |
| F-2 | Etherscan 링크 클릭 | 새 탭에서 Sepolia Etherscan 트랜잭션 페이지 열림 | |
| F-3 | 트랜잭션 완료 후 "닫기" | TxPending 오버레이 닫힘 | |

---

## 테스트 결과 요약

| 섹션 | 전체 | 통과 | 실패 |
|------|------|------|------|
| A. 지갑 연결 | 7 | | |
| B. 카테고리 관리 | 8 | | |
| C. 할 일 추가 | 10 | | |
| D. 편집/삭제/완료 | 8 | | |
| E. 필터 & 정렬 | 9 | | |
| F. 에러 처리 | 3 | | |
| **합계** | **45** | | |

---

> ✅ 정상 / ❌ 오류 (오류 내용 기록)
